<!DOCTYPE html>
<html>
  <head>
    <title>CarbonByte</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
  </head>

  <body class="bg-[#f0f2f5]">
  <% if user_signed_in? %>
        <%= render partial: 'layouts/navbar' %>
        <% else %>
      <nav class="bg-white dark:bg-gray-900 fixed w-full z-20 top-0 start-0 border-b border-gray-200 dark:border-gray-600">
        <div class="max-w-screen-xl flex items-center justify-start mx-auto p-4">
          <div class="flex items-center space-x-3 rtl:space-x-reverse">
            <img src="https://play-lh.googleusercontent.com/FCZI51N25UViDPXNe_e7LtyN30x-EH61hY_1hINY_1S3F06yzQ3XFlFkrikgRwO_0q7Y" class="h-8" alt="CarbonByte Logo">
            <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">CarbonByte</span>
          </div>
        </div>
      </nav>
      <% end %>
    <main class="container mx-auto mt-28 px-5 flex flex-col items-center">
    <div class="flex flex-col items-center px-20">
      <% flash.each do |type, msg| %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative custom-notice">
        <%= msg %>
        </div>
      <% end %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>